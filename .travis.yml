language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm run travis
  - npm run test -- --coverage # Include coverage when running tests
  - npm run build
after_script: # Upload coverage reports
  - COVERALLS_REPO_TOKEN=$coveralls_repo_token npm run coveralls
deploy:
  provider: pages
  skip_cleanup: true
  github_token: a026741700ccb51002daed89527b9bf16446fb8d
  local_dir: react-backend/client/build
  on:
    branch: master
