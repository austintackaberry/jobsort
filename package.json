{
  "scripts": {
    "start": "cd react-backend && npm install && npm start && cd client && npm install",
    "build": "cd react-backend/client && npm run build"
  },
  "dependencies": {
    "node-fetch": "^1.7.3"
  }
}
